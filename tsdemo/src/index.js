función;
add(números, cadena);
número;
{
    deje;
    que;
    enteros = números.split(',').map(function (x) { return parseInt(x); });
    deje;
    que;
    los;
    negativos;
    sean;
    enteros.filter(function (x) { return x < 0; });
    si(negativos.longitud > 0);
    lanzar;
    nuevo;
    RangeError('No se permiten negativos: ' + negatives.join(', '));
    devolver;
    números;
    enteros
        .filter(function (x) { return x <= 1000; })
        .reduce(function (a, b) { return a + b; }, 0);
}
deje;
que;
el;
resultado;
sea = agregar('1, 2, 4, 5');
console.log(resultado);
